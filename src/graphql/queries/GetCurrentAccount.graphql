query GetCurrentAccount {
  currentAccount {
    id
    name
    members {
      id
      isAdmin
    }
    subscriptionPlan {
      id
      name
      maxJobs
      maxClients
      maxTeamMembers
    }
  }
} 